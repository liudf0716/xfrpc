[common]
server_addr = 127.0.0.1
server_port = 7000
token = test-quic-token-2026

# Use QUIC transport
protocol = quic

# frps QUIC bind port (must match frps.toml quicBindPort)
quic_bind_port = 7002

[ssh]
type = tcp
local_ip = 127.0.0.1
local_port = 22
remote_port = 6000
